#include "UI.h"
#include "lcd.h"
#include "stm32f10x.h"
#include <stdio.h>

// ??????????
void UI_Refresh_Data(float temp, float humi) {
    char str_temp[10], str_humi[10];
    
    // 1. ?????
    sprintf(str_temp, "%.1f C", temp);
    sprintf(str_humi, "%.1f %%", humi);
    
    // 2. ??????(??“????”)
    // ??:???? lcd.h ??? RED ? GREEN ???,????? 0xF800(?) ? 0x07E0(?) ??
    uint16_t temp_color = (temp > 30.0f) ? RED : GREEN; 
    
    // 3. ?? LCD ?? API ??????
    // ???? (const u8 *) ??????;???? 0 ?????????
    LCD_ShowString(120, 100, (const u8 *)str_temp, temp_color, BLACK, 24, 0);
    LCD_ShowString(120, 140, (const u8 *)str_humi, CYAN, BLACK, 24, 0);
}

